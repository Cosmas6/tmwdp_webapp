rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if
          true;
    }
    
    //match auth to guides collection
    // match /guides/{guideId} {
    // allow read,write : if request.auth.null != null
    // }
  }
}