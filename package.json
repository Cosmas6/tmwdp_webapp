{
  "name": "tmwdp_webapp",
  "version": "0.1.0",
  "description": "",
  "proxy": "http://localhost:8080",
  "main": "./client/webpack.config.js",
  "engines": {
    "node": "16.13.1"
  },
  "dependencies": {
    "webpack": "^5.70.0"
  },
  "scripts": {
    "build": "cd client && webpack --mode=production",
    "watch": "cd client && webpack --watch",
    "start": "cd client && webpack serve --open --mode=development"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
  }
}
